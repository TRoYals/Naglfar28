---
date: 2026-06-21
---

# 2026-06-21

## maimai 同步

- 自动化读取 `26-6-20_21-15-5.log`，抽取 5 条 `UploadUserPlaylogApi Request.userPlaylog`，全部完成度 >= 95%，并增量写入 `游玩历史.loom`。
- 按 `musicId + 难度` 刷新 `maimai数据库.loom`：历史表 49 行、汇总表 43 行；`不機嫌なスリーカード` Master 聚合为 2 次游玩，其余 4 个曲目/难度组合新增。
- 保留数据库手动维护字段与现有 tag 选项；校验通过：JSON 可解析、无 UTF-8 BOM、无冲突标记，tag 选项使用 `content`，date 列保持 `yyyymmdd` 与 `includeTime:false`。
## maimai 同步

- 自动化读取 `26-6-21_12-28-36.log`，抽取 5 条 `UploadUserPlaylogApi Request.userPlaylog`，全部完成度 >= 95%，并按 `userPlayDate + musicId + level + trackNo + achievement + deluxscore` 去重后写入 `游玩历史.loom`。
- 按 `musicId + 难度` 刷新 `maimai数据库.loom`：历史表 54 行，汇总表 44 行；新增 `スローグロー` Master 汇总行，更新 `アイアイ傘`、`RIFFRAIN`、`Falling`、`ケロ⑨destiny` 的最近游玩、游玩次数、最高完成度、单曲积分和谱面元数据。
- 对本次涉及且缺标签的曲目补充练习标签，并尽量补入 B 站谱面/教学入口；保留已有手动维护字段和用户新增的 `谱面教学链接` 列。
- 校验通过：JSON 可解析、无 UTF-8 BOM、无冲突标记、tag 选项使用 `content`，date 列保持 `yyyymmdd` 且 `includeTime:false`。

## maimai 配置术语整理

- 根据 B 站合集「舞萌配置术语」的 3 个视频标题、简介和扩展阅读 BV 号，新建 `舞萌配置术语刻意练习跟踪.md`。
- 文章先整理通用配置、死镰段/水神段/如龙段、星星篇相关术语，并用表格跟踪识别要点、刻意练习目标、关联标签、状态和参考链接。
- 由于视频没有公开字幕轨，正文标注为先按元数据整理，后续看视频时补充时间点、例曲、正攻/邪道手法。

## maimai 同步

- 自动化读取 `26-6-21_15-39-59.log`，抽取 5 条 `UploadUserPlaylogApi Request.userPlaylog`，全部完成度 >= 95%，并按 `userPlayDate + musicId + level + trackNo + achievement + deluxscore` 去重后写入 `游玩历史.loom`。
- 按 `musicId + 难度` 刷新 `maimai数据库.loom`：历史表 59 行，汇总表 46 行；新增 `with U` Master 与 `白ゆき` Master 汇总行，更新 `EVERGREEN`、`妄想感傷代償連盟`、`うぇいびー` 的最近游玩、游玩次数、最高完成度、单曲积分和谱面元数据。
- 对本次涉及且缺标签的曲目补充练习标签，并补入 B 站谱面入口：`with U` 标记星星，`妄想感傷代償連盟` 标记换手，`白ゆき` 标记大节奏，`うぇいびー` 标记散打；保留已有手动维护字段和现有 tag 选项。
- 校验通过：JSON 可解析、无 UTF-8 BOM、无冲突标记、tag 选项使用 `content`，date 列保持 `yyyymmdd` 且 `includeTime:false`。
