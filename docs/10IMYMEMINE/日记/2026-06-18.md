---
date: 2026-06-18
---

# 2026-06-18

## maimai 同步

- 自动化读取 `26-6-18_18-43-23.log`，新增 10 条 2026-06-18 游玩流水到 `游玩历史.loom`。
- 按 `musicId + 难度` 刷新 `maimai数据库.loom` 汇总，保留标签、short note、break、状态、备注等手动维护字段。
- 本次校验通过：Loom JSON 可解析、无 UTF-8 BOM、无冲突标记，tag 选项使用 `content`，date 列保持 `yyyymmdd` 与 `includeTime:false`。
