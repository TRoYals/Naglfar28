---
date: 2026-06-24
---

# 2026-06-24

## maimai 同步

- 自动化扫描 `D:\MAIMAI\maimai\Package\MelonLoader\Logs` 中的候选 MelonLoader 日志；最新 `26-6-24_0-33-59.log` 只有设置、排行榜、活动、全国达成率等请求，没有 `UploadUserPlaylogApi Request.userPlaylog`，因此本轮没有新增游玩记录。
- 当前 `游玩历史.loom` 已有时间段覆盖：2026-06-16 01:09:20-09:47:54、2026-06-17 00:13:38-01:01:43、2026-06-18 19:47:05-20:18:10、2026-06-20 15:17:56-22:33:21、2026-06-21 13:30:15-16:53:47；候选日志本轮未覆盖新的 `userPlayDate` 成绩区间。
- 校验 maimai Loom 文件：JSON 可解析、无 UTF-8 BOM、无冲突标记、tag 选项使用 `content`，date 列保持 `yyyymmdd` 与 `includeTime:false`。本轮未重写 Loom；保留已有手动维护字段和当前未提交的 `with U` Master 标签调整。
## maimai 同步

- 自动化扫描 `D:\MAIMAI\maimai\Package\MelonLoader\Logs`，本轮使用候选日志 `26-6-24_0-42-42.log`，实际覆盖 `userPlayDate` 2026-06-24 01:46:11-02:19:34。日志内 10 条 `UploadUserPlaylogApi Request.userPlaylog` 中，按“只记录 95% 以上完成度”规则写入 8 条，跳过 2 条低于 95% 的记录。
- `游玩历史.loom` 新增 8 条流水：うぇいびー、MOBILYS、ナミダと流星、ケロ⑨destiny、トリアージ、妄想感傷代償連盟、天火明命、No Routine。当天历史表覆盖时间段为 2026-06-24 01:46:11-02:19:34。
- `maimai数据库.loom` 按 `musicId + 难度` 聚合刷新 8 个谱面行，其中新增 MOBILYS Master、ナミダと流星 Master、天火明命 Expert、No Routine Master；保留已有手工字段，并为缺标签谱面补了练习标签和 Bilibili 谱面链接。
- 校验 maimai Loom 文件：JSON 可解析、无 UTF-8 BOM、无冲突标记、tag 选项使用 `content`，date 列保持 `yyyymmdd` 与 `includeTime:false`。

## maimai 同步规格调整

- 按用户要求为 `游玩历史.loom` 和 `maimai数据库.loom` 增加 `谱面类型` tag 列，选项为 `标准`、`DX`、`宴会`。
- 已按 `musicId >= 100000 => 宴会`、`musicId >= 10000 => DX`、其它为 `标准` 回填现有历史和数据库行。
- 已更新 `Maimai Agent 同步说明.md`：后续自动化写入历史表时要填充 `谱面类型`，聚合数据库时使用 `musicId + 谱面类型 + 难度` 定位，避免 DX 谱和标准谱互相覆盖。
## maimai 数据库补链

- 按要求为 `maimai数据库.loom` 中所有空的 `谱面教学链接` 补齐链接，共补 40 行。
- 链接采用 Bilibili 精确搜索链接，关键词包含曲名、谱面类型、难度和谱面确认；已有直链未覆盖。
- 复核后数据库中 `谱面教学链接` 已无空值，链接均为 Markdown `[]()` 格式。

## maimai 同步

- 自动化扫描 `D:\MAIMAI\maimai\Package\MelonLoader\Logs`，本轮使用候选日志 `26-6-24_12-41-7.log`，实际覆盖 `userPlayDate` 2026-06-24 13:45:25.0-2026-06-24 14:00:56.0。日志内 5 条 95% 以上 `UploadUserPlaylogApi Request.userPlaylog` 中，新增写入 5 条。
- `游玩历史.loom` 新增 5 条流水：No Routine 标准 Master、Stardust Memories DX Master、Colorful Starting Line DX Master、彩りキャンバス DX Master、Lividi 标准 Expert。当天历史表当前覆盖时间为 01:46:11-14:00:56。
- `maimai数据库.loom` 按 `musicId + 谱面类型 + 难度` 聚合刷新；本轮新增汇总行 2 行，并为标签为空的谱面补充练习标签：Stardust Memories: 星星、彩りキャンバス: 星星、Lividi: 散打
- 校验目标保持为 JSON 可解析、无 UTF-8 BOM、无冲突标记、tag 选项使用 `content`，date 列使用 `yyyymmdd` 且 `includeTime:false`。
